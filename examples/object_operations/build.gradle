plugins {
    id 'java'
}

description = 'Ballerina - AWS S3 Object Operations Example'

task clean {
    delete 'target'
    delete 'Dependencies.toml'
}

task build {
    dependsOn clean
    doLast {
        exec {
            workingDir project.projectDir
            commandLine 'bal', 'build'
        }
    }
}

task run {
    doLast {
        exec {
            workingDir project.projectDir
            commandLine 'bal', 'run'
        }
    }
}